apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brc20-programmable-module.fullname" . }}-test-connection"
  labels:
    {{- include "brc20-programmable-module.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brc20-programmable-module.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
