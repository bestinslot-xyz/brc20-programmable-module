apiVersion: v1
kind: Service
metadata:
  name: {{ include "brc20-programmable-module.fullname" . }}
  labels:
    {{- include "brc20-programmable-module.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "brc20-programmable-module.selectorLabels" . | nindent 4 }}
