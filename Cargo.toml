[package]
name = "brc20_prog"
version = "0.1.0"
edition = "2021"

[dependencies]
db = { path = "db" }
server = { path = "server" }
tokio = "1.43.0"

[workspace]
members = [ ".", "db", "server" ]

[profile.release]
debug = true
